# apps/web

Next.js + TypeScript UI (Phase 5). Routes: `/dashboard`, `/risk`, `/planner`, `/shadow`, `/failures`,
`/provenance`, `/reliability`, `/admin`. It consumes the FastAPI service in `apps/api`.
